[project]
name = "sync-worktree"
version = "0.4.5"
description = "Config-driven file sync between git worktrees"
requires-python = ">=3.8"
license = "MIT"

[project.scripts]
sync-worktree = "sync_worktree:main"
