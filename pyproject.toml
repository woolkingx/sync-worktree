[project]
name = "sync-worktree"
version = "0.5.18"
description = "AI decision support for git worktree operations with bounded reports"
requires-python = ">=3.8"
license = "MIT"

[project.scripts]
sync-worktree = "sync_worktree:main"
